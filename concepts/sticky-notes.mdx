---
title: "Text notes"
description: "Use text notes for annotations, reminders, and visual wayfinding on the NodePad canvas."
---

import { FeatureGrid } from '/snippets/FeatureGrid.jsx';
import { NumberedSteps } from '/snippets/NumberedSteps.jsx';
import { Callout } from '/snippets/Callout.jsx';
import { ProductScreenshot } from '/snippets/ProductScreenshot.jsx';

Text notes are freeform notes that live on the canvas beside your conversation branches. They help you annotate the workspace, keep track of decisions, and leave yourself or teammates breadcrumbs about what to do next.

<ProductScreenshot
  light="/images/placeholder-light.svg"
  dark="/images/placeholder-dark.svg"
  alt="A canvas with two branches and a text note labeling the cluster."
/>

## What text notes are for

<FeatureGrid
  cols={2}
  items={[
    { title: 'Session notes', body: 'Capture what the workspace is for, what you are testing, or what still needs review.' },
    { title: 'Checklists', body: 'Keep a short list of open questions, next actions, or review criteria.' },
    { title: 'Wayfinding', body: 'Label clusters of branches so a large workspace stays readable when you zoom out.' },
    { title: 'Human context', body: 'Leave notes for collaborators that should stay visible on the canvas but out of model prompts.' },
  ]}
/>

## How to create one

<NumberedSteps
  items={[
    { title: 'Right-click empty canvas space', body: 'Open the canvas context menu where you want the note to appear.' },
    { title: 'Choose New text note here', body: 'NodePad drops a new note at that position.' },
    { title: 'Edit the note', body: 'Type whatever annotation or checklist you need and place it near the relevant branches.' },
  ]}
/>

## What text notes do not do

Text notes are not model instructions. They are not automatically injected into the prompt context for nearby branches.

If you want instructions to affect the model, put them in:

- your message
- workspace instructions
- a branching strategy that keeps different prompt intents separate

## Good note patterns

<AccordionGroup>
  <Accordion title="Workspace header note">
    Put one note near the top-left of a workspace that says what the entire canvas is trying to solve.
  </Accordion>
  <Accordion title="Decision log">
    Keep a note beside a cluster of branches that records the choice you made after comparing them.
  </Accordion>
  <Accordion title="Review checklist">
    Add a note with the standards you want to check before exporting, sharing, or shipping the result.
  </Accordion>
</AccordionGroup>

<Callout tone="note">
  Earlier drafts of these docs described text notes as prompt constraints. In the current product, they are canvas annotations for humans.
</Callout>
