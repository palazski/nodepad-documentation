---
title: "Text notes"
description: "Annotations, reminders, and visual wayfinding on the NodePad canvas."
---

import { NavList } from '/snippets/NavList.jsx';

Text notes are freeform notes that live on the canvas beside your conversation branches. They help you annotate the workspace, keep track of decisions, and leave yourself or teammates breadcrumbs about what to do next.

## What text notes are for

- **Session notes.** Capture what the workspace is for, what you are testing, or what still needs review.
- **Checklists.** Keep a short list of open questions, next actions, or review criteria.
- **Wayfinding.** Label clusters of branches so a large workspace stays readable when you zoom out.
- **Human context.** Leave notes for collaborators that should stay visible on the canvas but out of model prompts.

## How to create one

1. Right-click empty canvas space to open the canvas context menu.
2. Choose **New text note here**. NodePad drops a new note at that position.
3. Type whatever annotation or checklist you need and place it near the relevant branches.

## What text notes do not do

Text notes are not model instructions. They are not automatically injected into the prompt context for nearby branches. If you want instructions to affect the model, put them in your message, in the workspace instructions, or in a branching strategy that keeps different prompt intents separate.

## Patterns that work

<AccordionGroup>
  <Accordion title="Workspace header note">
    Put one note near the top-left of a workspace that says what the entire canvas is trying to solve.
  </Accordion>
  <Accordion title="Decision log">
    Keep a note beside a cluster of branches that records the choice you made after comparing them.
  </Accordion>
  <Accordion title="Review checklist">
    Add a note with the standards you want to check before exporting, sharing, or shipping the result.
  </Accordion>
</AccordionGroup>

> Earlier drafts of these docs described text notes as prompt constraints. In the current product, they are canvas annotations for humans.

## Continue

<NavList items={[
  { title: 'The canvas', body: 'How notes sit beside branches and edges in a workspace.', href: '/concepts/canvas' },
  { title: 'References', body: 'When you do want context to move into a prompt, use `@`.', href: '/concepts/references' },
]} />
