---
title: "Nodes"
description: "Nodes are the individual messages and replies that make up a branch in NodePad."
---

import { FeatureGrid } from '/snippets/FeatureGrid.jsx';
import { EdgeChip } from '/snippets/EdgeChip.jsx';
import { Callout } from '/snippets/Callout.jsx';
import { ProductScreenshot } from '/snippets/ProductScreenshot.jsx';

A node is one message on the canvas. User messages, assistant replies, and in-progress drafts all behave like nodes in the graph. Most NodePad actions start by selecting a node or branching from one.

## What a node can contain

<FeatureGrid
  cols={2}
  items={[
    { title: 'Message content', body: 'NodePad renders message text with Markdown, so lists, code blocks, and links stay readable inside the graph.' },
    { title: 'Attachments', body: "A node can carry uploaded files, images, and tool-call artifacts. Those attachments stay part of the node's context footprint." },
    { title: 'Model metadata', body: 'Assistant nodes show which model produced the reply. Workspace defaults control the starting point, but individual sends can override them.' },
    { title: 'References', body: 'A node can reference other messages with `@` mentions. The visible token is only the UI layer; NodePad stores the actual cross-message links separately.' },
  ]}
/>

## How nodes become branches

Nodes connect in sequence inside a branch. Each node-to-next-node link is a <EdgeChip type="continuation">continuation edge</EdgeChip>. When you branch from a node, NodePad creates a new path from that point — a <EdgeChip type="fork">fork edge</EdgeChip> — without changing the original branch.

<ProductScreenshot
  light="/images/concepts/four-node-light.svg"
  dark="/images/concepts/four-node-dark.svg"
  alt="Four nodes in a chain: you → assistant → you → assistant, with a forked branch starting from the last node."
/>

## Actions you can take from a node

<FeatureGrid
  cols={3}
  items={[
    { title: 'Branch from here', body: 'Start a new branch from the exact context at that point.', href: '/concepts/forking' },
    { title: 'Edit or re-run', body: 'Depending on the node type and position, you can edit, re-send, or re-run from that point.' },
    { title: 'Reference or merge', body: 'Pull this node into another message through an `@` reference or a merge flow.', href: '/concepts/references' },
  ]}
/>

## Important behaviors to know

<AccordionGroup>
  <Accordion title="Branches preserve history">
    Branching does not rewrite the original path. It creates a new one.
  </Accordion>
  <Accordion title="References are explicit">
    Mentioning `@B1-2` in text is not enough on its own. The reference picker records the actual linked node so NodePad knows what to include.
  </Accordion>
  <Accordion title="Node IDs are useful">
    You can copy a node ID from the context menu. That is handy when you want to reference a specific message cleanly.
  </Accordion>
  <Accordion title="Text notes are different">
    Text notes are not conversation nodes. They are standalone annotations on the canvas.
  </Accordion>
</AccordionGroup>

<Callout tone="tip">
  If you are unsure whether a follow-up belongs in the current branch, branch first. It is cheaper to keep two clean paths than to untangle one noisy one later.
</Callout>
