---
title: "Nodes"
description: "Individual messages and replies that make up a branch."
---

import { EdgeChip } from '/snippets/EdgeChip.jsx';
import { NavList } from '/snippets/NavList.jsx';
import { ProductScreenshot } from '/snippets/ProductScreenshot.jsx';

A node is one message on the canvas. User messages, assistant replies, and in-progress drafts all behave like nodes in the graph. Most NodePad actions start by selecting a node or branching from one.

## What a node carries

Message content rendered as Markdown — lists, code blocks, links — so the message stays readable inside the graph. Attachments: uploaded files, images, and tool-call artifacts that stay part of the node's context footprint. Model metadata: assistant nodes show which model produced the reply. References: a node can reference other messages with `@` mentions; the visible token is only the UI layer, the actual cross-message link is stored separately.

## How nodes become branches

Nodes connect in sequence. Each link forward is a <EdgeChip type="continuation">continuation edge</EdgeChip>. When you branch from a node, NodePad creates a new path from that point — a <EdgeChip type="fork">fork edge</EdgeChip> — without changing the original branch.

<ProductScreenshot
  light="/images/concepts/four-node-light.svg"
  dark="/images/concepts/four-node-dark.svg"
  alt="Four nodes in a chain — you, assistant, you, assistant — with a forked branch starting from the last node."
/>

## What you can do from a node

From any node, you can branch — start a new path from the exact context at that point. You can edit or re-run, depending on the node type and position. You can pull this node into another message through an `@` reference or a merge flow.

## Behaviors worth knowing

<AccordionGroup>
  <Accordion title="Branches preserve history">
    Branching does not rewrite the original path. It creates a new one.
  </Accordion>
  <Accordion title="References are explicit">
    Mentioning `@B1-2` in text is not enough on its own. The reference picker records the actual linked node so NodePad knows what to include.
  </Accordion>
  <Accordion title="Node IDs are useful">
    You can copy a node ID from the context menu. That is handy when you want to reference a specific message cleanly.
  </Accordion>
  <Accordion title="Text notes are different">
    Text notes are not conversation nodes. They are standalone annotations on the canvas.
  </Accordion>
</AccordionGroup>

> If you are unsure whether a follow-up belongs in the current branch, branch first. It is cheaper to keep two clean paths than to untangle one noisy one later.

## Continue

<NavList items={[
  { title: 'Forking', body: 'When and how to branch.', href: '/concepts/forking' },
  { title: 'References', body: 'How `@` works across branches.', href: '/concepts/references' },
]} />
