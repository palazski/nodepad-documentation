---
title: "Forking"
description: "Branch from any message so you can explore a new path without overwriting the current one."
---

import { FeatureGrid } from '/snippets/FeatureGrid.jsx';
import { NumberedSteps } from '/snippets/NumberedSteps.jsx';
import { EdgeChip } from '/snippets/EdgeChip.jsx';
import { Callout } from '/snippets/Callout.jsx';
import { ProductScreenshot } from '/snippets/ProductScreenshot.jsx';

Forking is how NodePad turns one conversation into two. Instead of asking every follow-up in the same branch, you can split at a specific message and test another direction beside it. Each split appears on the canvas as a <EdgeChip type="fork">fork edge</EdgeChip>.

<ProductScreenshot
  light="/images/placeholder-light.svg"
  dark="/images/placeholder-dark.svg"
  alt="Two branches diverging from the same message, with a blue fork edge between them."
/>

## When to fork

<FeatureGrid
  cols={2}
  accent="fork"
  items={[
    { title: 'Get a second opinion', body: 'Ask another model the same question before you commit to a decision.' },
    { title: 'Chase a tangent', body: 'Explore a side question without polluting the branch you still care about.' },
    { title: 'Compare prompts', body: 'Send two different follow-ups from the same point and see which framing works better.' },
    { title: 'Preserve a checkpoint', body: 'Split before a risky change so the original path stays intact.' },
  ]}
/>

## How branching works

<NumberedSteps
  items={[
    { title: 'Pick the message to branch from', body: 'Right-click the node that represents the context you want to preserve.' },
    { title: 'Choose Branch from here', body: 'NodePad creates a new draft connected to that source node.' },
    { title: 'Send a new follow-up', body: 'The new draft starts from the same history up to that point, then diverges based on what you send next.' },
    { title: 'Compare both branches on the canvas', body: 'Because both branches stay visible, you can compare results without switching tabs or losing provenance.' },
  ]}
/>

## Branching patterns that work well

<AccordionGroup>
  <Accordion title="Same question, different model">
    Branch from a user message, keep the wording the same, and route the branch to another model. This is useful when you want a true second read on the same prompt.
  </Accordion>
  <Accordion title="Same branch, tighter scope">
    Branch from a broad response and ask one narrower question in the new branch. The main branch stays high level while the new one goes deep.
  </Accordion>
  <Accordion title="Re-send versus branch">
    Re-send is useful when you want to try the same message again. Branching is better when you want an alternate path you plan to keep around.
  </Accordion>
  <Accordion title="Branch before merge work">
    If you know a branch is going to become source material for another message later, branching early keeps those sources cleaner and easier to reference.
  </Accordion>
</AccordionGroup>

<Callout tone="info">
  Branches are explicit graph structure. They are not just copied text in a new chat window.
</Callout>
