---
title: "What is NodePad? Your canvas workspace for AI chat"
description: "NodePad replaces the linear AI chat thread with a spatial canvas where you run multiple conversations, fork branches, and merge results side by side."
---

NodePad is a canvas workspace for AI chat, made by The Palaz Company. Instead of one scrolling thread, you work on an open canvas where you can run multiple AI conversations side by side, branch any message into its own thread, and merge results back together — all without losing track of where an idea came from.

## The problem with linear chat

Every major AI assistant gives you the same interface: one message, one reply, repeat. That works for simple questions, but it breaks down when you want to explore more than one direction at once, compare answers from different models, or keep a complex project organized across dozens of exchanges. You end up with a wall of text that's hard to navigate and impossible to restructure.

NodePad replaces the scroll with a canvas. Your conversations become objects you can move, connect, and manipulate — more like a thinking tool than a chatbox.

## Three core moves

NodePad is built around three actions that you'll come back to constantly.

<Steps>
  <Step title="Chat">
    Open the canvas and start typing. Your first node works exactly like any AI chat — no new concepts required. Pick any supported model (Claude or GPT) and send your first message.
  </Step>
  <Step title="Extend to multiple chats">
    When a thread sparks a new question or you want to explore a different angle, spawn a second chat right on the canvas. Both conversations stay visible and spatially related, so context doesn't get lost between browser tabs.
  </Step>
  <Step title="Manipulate context">
    Fork any message to branch the conversation from that exact point, add sticky notes to nodes to pin constraints the model should always respect, and merge threads together once you've reached a conclusion worth keeping.
  </Step>
</Steps>

## What you can do in NodePad

<CardGroup cols={2}>
  <Card title="Quick start" icon="rocket" href="/quickstart">
    Send your first message and spawn your first second thread in under five minutes.
  </Card>
  <Card title="Canvas and nodes" icon="layout-dashboard" href="/concepts/canvas">
    Understand how the canvas is organized and how nodes relate to each other.
  </Card>
  <Card title="Forking and merging" icon="git-fork" href="/concepts/forking">
    Branch conversations at any message and combine threads when you're ready.
  </Card>
  <Card title="Supported models" icon="microchip" href="/models/overview">
    Compare Claude and GPT side by side — switchable per message.
  </Card>
  <Card title="Sticky notes" icon="sticky-note" href="/concepts/sticky-notes">
    Pin context constraints to any node so the model never forgets your rules.
  </Card>
  <Card title="Enterprise" icon="building" href="/enterprise/overview">
    Self-host NodePad inside your own infrastructure.
  </Card>
</CardGroup>

<Note>
  NodePad is currently in beta. [Request access](https://node-pad.com/beta) to try it free — see [Beta access](/beta-access) for details on how onboarding works.
</Note>
